# 💳 Интеграция оплаты через Tribute

## ✅ Что реализовано

1. **Страница успешной оплаты** (`/success`)
   - Красивая страница благодарности после оплаты
   - Анимации и современный дизайн
   - Кнопки для возврата на главную

2. **Webhook обработчик** (`/api/tribute-webhook`)
   - Принимает POST запросы от Tribute
   - Проверяет статус платежа `status === "paid"`
   - Логирует данные в консоль
   - Готов для интеграции с БД и email

3. **Обновлены все кнопки оплаты**
   - Все CTA кнопки ведут на `https://web.tribute.tg/p/pm9H`
   - Единообразный стиль с иконкой 💳
   - Текст: "💳 Оплатить курс $33"

4. **Конфигурация** (`config/payment.ts`)
   - Централизованное управление ссылками
   - Легко изменить ID продукта

## 🚀 Быстрый старт

### 1. Настройте ссылку на оплату

Откройте `config/payment.ts` и замените `pm9H` на ваш реальный ID продукта:

```typescript
export const PAYMENT_LINK_ID = 'ваш-id-продукта'
```

### 2. Настройте Webhook в Tribute

1. Войдите в Tribute → настройки продукта
2. Webhook URL: `https://your-domain.vercel.app/api/tribute-webhook`
3. Redirect URL: `https://your-domain.vercel.app/success`

### 3. Деплой на Vercel

```bash
# Подключите репозиторий к Vercel
# После деплоя обновите webhook URL в Tribute
```

## 📝 Структура файлов

```
app/
├── success/
│   └── page.tsx          # Страница успешной оплаты
├── api/
│   └── tribute-webhook/
│       └── route.ts       # Обработчик webhook
config/
└── payment.ts            # Конфигурация ссылок
components/
├── PaymentButton.tsx      # Переиспользуемый компонент кнопки
└── ...                    # Остальные компоненты обновлены
```

## 🔧 Дополнительные настройки

Подробная инструкция в файле **[TRIBUTE_SETUP.md](./TRIBUTE_SETUP.md)**

## 📊 Проверка работы

После настройки webhook проверьте:

```bash
# GET запрос (должен вернуть { ok: true })
curl https://your-domain.vercel.app/api/tribute-webhook
```

## 🎯 Что происходит при оплате

1. Пользователь нажимает "💳 Оплатить курс $33"
2. Переходит на страницу оплаты Tribute
3. После оплаты:
   - Tribute отправляет POST на `/api/tribute-webhook`
   - Пользователь перенаправляется на `/success`
4. Webhook обрабатывает платеж и логирует данные

## ⚠️ Важно

- Замените `pm9H` на ваш реальный ID продукта
- Обновите webhook URL после деплоя на Vercel
- Проверьте логи в Vercel Dashboard после тестовых платежей

