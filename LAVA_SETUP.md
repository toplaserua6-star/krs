# 💳 Настройка оплаты через Lava.top

## ✅ Что реализовано

1. **Виджет оплаты Lava.top** (`components/LavaPaymentWidget.tsx`)
   - Встроенный iframe виджет
   - Автоматический fallback на кнопку, если виджет не загружается
   - Анимации и стилизация

2. **Страница успешной оплаты** (`/success`)
   - Уже настроена и готова к работе

3. **Интеграция во все компоненты**
   - Hero, CTA, Pricing
   - HeroMarketers, CTAMarketers

## 🚀 Быстрый старт

### 1. Получите ID виджета из Lava.top

1. Войдите в ваш аккаунт Lava.top
2. Создайте продукт или выберите существующий
3. Скопируйте код iframe, например:
   ```html
   <iframe src="https://widget.lava.top/ac7bb94f-665f-4001-bce6-df8894bb4129"></iframe>
   ```
4. ID виджета - это часть после последнего слеша: `ac7bb94f-665f-4001-bce6-df8894bb4129`

### 2. Настройте ID в проекте

Откройте файл `config/lava.ts` и замените ID:

```typescript
// Было:
export const LAVA_WIDGET_ID = 'ac7bb94f-665f-4001-bce6-df8894bb4129'

// Стало (пример):
export const LAVA_WIDGET_ID = 'ваш-реальный-id-виджета'
```

### 3. Настройте Redirect URL в Lava.top

1. Перейдите в настройки вашего продукта в Lava.top
2. Найдите раздел "Redirect URL" или "URL перенаправления"
3. Укажите:
   ```
   https://your-domain.vercel.app/success
   ```
   (Замените `your-domain.vercel.app` на ваш реальный домен)

4. Сохраните настройки

### 4. Деплой на Vercel

1. Подключите репозиторий к Vercel
2. После деплоя обновите Redirect URL в Lava.top на новый домен

## 🔧 Решение проблем

### Виджет не загружается (ошибка подключения)

**Причины:**
1. Неправильный ID виджета
2. Виджет не активирован в Lava.top
3. Проблемы с сетью/CORS

**Решение:**
- Компонент автоматически покажет альтернативную кнопку оплаты
- Кнопка ведёт напрямую на страницу оплаты Lava.top
- Проверьте ID виджета в `config/lava.ts`

### Виджет не отображается на localhost

**Причина:** Lava.top может требовать HTTPS для работы виджета

**Решение:**
- Используйте ngrok для тестирования на localhost:
  ```bash
  ngrok http 3000
  ```
- Или протестируйте после деплоя на Vercel

### Виджет загружается, но оплата не работает

**Проверьте:**
1. Правильно ли настроен Redirect URL в Lava.top
2. Активирован ли продукт в Lava.top
3. Есть ли баланс/настройки платежей в Lava.top

## 📝 Структура файлов

```
components/
├── LavaPaymentWidget.tsx   # Виджет оплаты
config/
├── lava.ts                  # Конфигурация (ID виджета)
app/
├── success/
│   └── page.tsx            # Страница успешной оплаты
```

## 🎯 Как это работает

1. Пользователь видит iframe виджет Lava.top на странице
2. Нажимает кнопку оплаты внутри виджета
3. Оплата проходит через Lava.top
4. После успешной оплаты Lava.top перенаправляет на `/success`
5. Пользователь видит страницу благодарности

## 💡 Альтернативный вариант

Если виджет не работает, компонент автоматически показывает кнопку, которая ведёт напрямую на страницу оплаты Lava.top:

```typescript
// В компоненте уже есть fallback:
<a href="https://widget.lava.top/YOUR-WIDGET-ID">
  💳 Оплатить курс $33
</a>
```

## ⚠️ Важные замечания

1. **HTTPS обязателен** для работы виджета в production
2. **Redirect URL** должен точно совпадать с вашим доменом
3. **ID виджета** должен быть активным в Lava.top
4. Виджет работает только на том домене, который указан в настройках Lava.top

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что ID виджета правильный
3. Проверьте настройки в панели Lava.top
4. Убедитесь, что используете HTTPS в production

